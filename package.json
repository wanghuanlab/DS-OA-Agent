{
  "name": "ds-oa-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "install:browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "@playwright/test": "^1.45.0",
    "fastify": "^4.28.1",
    "node-cron": "^3.0.3",
    "open": "^10.1.0"
  }
}
